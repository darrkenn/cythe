base: rust:slim-trixie
track: main

steps:
  - name: Update
    run: apt-get update
  - name: Install git
    run: apt-get install -y git pkg-config libssl-dev build-essential
  - name: Checkout repo
    use: cythe-checkout

  - name: Run tests
    run: cargo test --verbose
